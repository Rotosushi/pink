
TODOs in no particular order:

	# finish v0.0.1
		needs:
			Parser
			Primitive Operators (Kernel)
			REPL

	# refactor the aux/ directory into multiple subdirectories, or multiple directories
		perhaps aux/ and operators/ ?

	# refactor the makefile into a cmake file
