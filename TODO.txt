
TODOs in no particular order:

	# finish v0.0.1
		needs:
			REPL

	# refactor the makefile into a cmake file

	# find out why when i use -stdlib=libc++ ld.lld fails to find some llvm functions
		and when i use -stdlib=libstdc++ it does not.
