programming is about three things, 
  control flow 
  data layout
  operations

the programming language is exists to facilitate the ease of 
combining these three things to write programs.

what we are concerned with here is types and values.
and more specifically how to cleanly and safely create/use/update values.

a value is any 'thing' which 'exists' during runtime or comptime.

the Type of a value describes it's layout in memory. 

however that is not the full story. because there are more distinctions we can make